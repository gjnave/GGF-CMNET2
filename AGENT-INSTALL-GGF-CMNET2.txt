GGF CMNET2 COLORIZATION STUDIO
AI AGENT INSTALLATION HANDOFF

Purpose
-------
This file is for a user who wants an AI agent to install GGF CMNET2
Colorization Studio for them.

Use the Get Going Fast installation method: check the machine first, install
only what belongs to this app, keep everything in a clean app-local folder, and
leave the user with a simple way to start it.

The agent is not creating an installer product.
The agent is installing the app on the user's machine.

When the work is finished, the user should have:

1. The app installed in a clean GGF folder.
2. A simple click-to-run BAT file.
3. A short START-HERE text file.
4. A verified local browser launch.


What This App Is
----------------
GGF CMNET2 Colorization Studio is a local browser GUI for CMNET2 / ColorMNet.

In practical terms, this app colorizes black-and-white images and videos using
reference color images or reference frames.

- Image mode:
  The user provides a grayscale target image and a color reference image.

- Video mode:
  The user provides a grayscale target video and optional reference frames.

The app runs locally, uses CUDA PyTorch on NVIDIA GPUs, and writes outputs to
the installed app's `outputs` folder.


Important Instruction To The Agent
----------------------------------
Do not build an installer.
Do not package a new installer.
Do not create a complex deployment system.

Your job is to install the app for the user and leave them with a way to run it.


System Prerequisites
--------------------
Before installing, check for these system-level requirements:

- Windows 10/11
- NVIDIA GPU
- NVIDIA driver with CUDA-capable PyTorch support
- Python 3.10 available through the `py` launcher
- Git for Windows available on PATH
- curl.exe available on PATH
- Internet access

These are system prerequisites, not normal Python package dependencies.
Do not try to silently fix missing system prerequisites inside the app install.

Check quickly:

    git --version
    py -3.10 --version
    nvidia-smi
    curl.exe --version

If Python, Git, curl, or NVIDIA/CUDA support is missing or clearly broken, stop
and tell the user:

    Your system is missing one or more required setup items for local AI apps.
    Please go to:

    https://setup.getgoingfast.pro

    Use the automatic setup there, or follow its manual setup instructions.
    After that finishes, run this AI-agent install again.

Only continue once those prerequisites are present.


Ask The User Where To Install
-----------------------------
Ask the user:

    Which drive do you want to install GGF CMNET2 on?
    I recommend C: unless you want to use another drive with more free space.

If the user says "default", use C:.

Do not install into system folders such as:

- C:\Windows
- C:\Program Files
- C:\Program Files (x86)
- System32
- protected OS folders


GGF Install Layout
------------------
Install the app under the drive chosen by the user:

    <DRIVE>:\GGF\GGF-CMNET2\
        CLICK-ME-TO-RUN.bat
        START-HERE.txt
        GGF-CMNET2\

The GitHub repository must be cloned into:

    <DRIVE>:\GGF\GGF-CMNET2\GGF-CMNET2

Public repository:

    https://github.com/gjnave/GGF-CMNET2.git


Install Steps
-------------
Replace C: with the drive chosen by the user.

1. Create the folders:

    if not exist C:\GGF mkdir C:\GGF
    if not exist C:\GGF\GGF-CMNET2 mkdir C:\GGF\GGF-CMNET2

2. Clone or update the app source:

    If C:\GGF\GGF-CMNET2\GGF-CMNET2\.git exists:

        git -C C:\GGF\GGF-CMNET2\GGF-CMNET2 pull --ff-only

    Otherwise:

        git clone https://github.com/gjnave/GGF-CMNET2.git C:\GGF\GGF-CMNET2\GGF-CMNET2

3. Create the Python virtual environment:

        cd /d C:\GGF\GGF-CMNET2\GGF-CMNET2
        py -3.10 -m venv .venv

   If Python 3.10 is not available, stop and send the user to:

        https://setup.getgoingfast.pro

4. Install Python dependencies:

        call .venv\Scripts\activate.bat
        .venv\Scripts\python.exe -m pip install --upgrade pip
        .venv\Scripts\python.exe -m pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision torchaudio
        .venv\Scripts\python.exe -m pip install opencv-python pillow scikit-image tqdm numpy einops tensorboard gradio

5. Create model folders:

        if not exist weights mkdir weights
        if not exist models mkdir models
        if not exist models\checkpoints mkdir models\checkpoints

6. Download required model files with resumable curl.

   Use `curl.exe`, not browser downloads. Existing complete files can be reused.
   If a file is partial, rerun the same curl command; `-C -` resumes it.

   CMNET2 ColorMNet weights:

        curl.exe -L --fail --retry 12 --retry-delay 5 --retry-all-errors -C - -o weights\DINOv2FeatureV6_LocalAtten_s2_154000.pth https://github.com/dan64/cmnet2/releases/download/v1.0.0/DINOv2FeatureV6_LocalAtten_s2_154000.pth

        Expected size: 494884817 bytes

   DINOv2 ViT-S/14 weights:

        curl.exe -L --fail --retry 12 --retry-delay 5 --retry-all-errors -C - -o models\checkpoints\dinov2_vits14_pretrain.pth https://github.com/dan64/cmnet2/releases/download/v1.0.0/dinov2_vits14_pretrain.pth

        Expected size: 88283115 bytes

   ResNet18 weights:

        curl.exe -L --fail --retry 12 --retry-delay 5 --retry-all-errors -C - -o models\checkpoints\resnet18-5c106cde.pth https://github.com/dan64/cmnet2/releases/download/v1.0.0/resnet18-5c106cde.pth

        Expected size: 46827520 bytes

   ResNet50 weights:

        curl.exe -L --fail --retry 12 --retry-delay 5 --retry-all-errors -C - -o models\checkpoints\resnet50-19c8e357.pth https://github.com/dan64/cmnet2/releases/download/v1.0.0/resnet50-19c8e357.pth

        Expected size: 102502400 bytes

   DINOv2 source zip:

        curl.exe -L --fail --retry 12 --retry-delay 5 --retry-all-errors -C - -o models\facebookresearch_dinov2_main.zip https://github.com/dan64/cmnet2/releases/download/v1.0.0/facebookresearch_dinov2_main.zip

        Expected size: 1226660 bytes

7. Verify model file sizes:

        powershell -NoProfile -ExecutionPolicy Bypass -Command "Get-Item 'weights\DINOv2FeatureV6_LocalAtten_s2_154000.pth','models\checkpoints\dinov2_vits14_pretrain.pth','models\checkpoints\resnet18-5c106cde.pth','models\checkpoints\resnet50-19c8e357.pth','models\facebookresearch_dinov2_main.zip' | Select-Object FullName,Length"

   Confirm the sizes match:

        weights\DINOv2FeatureV6_LocalAtten_s2_154000.pth    494884817
        models\checkpoints\dinov2_vits14_pretrain.pth       88283115
        models\checkpoints\resnet18-5c106cde.pth            46827520
        models\checkpoints\resnet50-19c8e357.pth            102502400
        models\facebookresearch_dinov2_main.zip             1226660

   If any file is smaller than expected, rerun that file's curl command.
   If any file is larger than expected, delete that file and download it again.

8. Extract DINOv2 source if needed:

        powershell -NoProfile -ExecutionPolicy Bypass -Command "Expand-Archive -LiteralPath 'models\facebookresearch_dinov2_main.zip' -DestinationPath 'models' -Force"

   After extraction, this folder should exist:

        models\facebookresearch_dinov2_main

9. Create this run file:

    C:\GGF\GGF-CMNET2\CLICK-ME-TO-RUN.bat

   Exact content:

        @echo off
        setlocal
        cd /d "%~dp0GGF-CMNET2"
        call "%~dp0GGF-CMNET2\run.bat"

10. Create this short user instruction file:

    C:\GGF\GGF-CMNET2\START-HERE.txt

   Exact content:

        GGF CMNET2 Colorization Studio

        To start the app:
        Double-click CLICK-ME-TO-RUN.bat

        Installed app folder:
        C:\GGF\GGF-CMNET2

        Installed app files:
        C:\GGF\GGF-CMNET2\GGF-CMNET2

        Public GitHub page:
        https://github.com/gjnave/GGF-CMNET2

        The app opens in your browser. It starts at port 7867.


Smoke Test
----------
After installation, verify the app starts and CUDA is visible.

1. Check Python, PyTorch, and CUDA:

        cd /d C:\GGF\GGF-CMNET2\GGF-CMNET2
        .venv\Scripts\python.exe -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'NO CUDA')"

   Expected:

        True
        An NVIDIA GPU name

2. Check the app status function:

        .venv\Scripts\python.exe -c "import sys; sys.path.insert(0, 'app'); from cmnet2_runtime import status; print(status())"

   Confirm the status mentions the installed root folder and present model files.

3. Start the app:

        C:\GGF\GGF-CMNET2\CLICK-ME-TO-RUN.bat

4. Confirm a local browser app opens.

Expected URL:

        http://127.0.0.1:7867

5. Confirm the page shows:

        CMNET2 Colorization Studio

6. Optional deeper smoke test:
   Use Image mode with the sample files:

        Target image:
        C:\GGF\GGF-CMNET2\GGF-CMNET2\assets\image\image_bw.jpg

        Reference image:
        C:\GGF\GGF-CMNET2\GGF-CMNET2\assets\image\image_color_ref.jpg

Successful outputs are written to:

        C:\GGF\GGF-CMNET2\GGF-CMNET2\outputs


Troubleshooting
---------------
If Git, Python, or CUDA/NVIDIA support is missing:

Stop and send the user to:

    https://setup.getgoingfast.pro

Tell them to use the automatic setup or manual setup instructions there, then
rerun this install after their system is ready.

If model download fails:

Rerun the failed `curl.exe` command. The `-C -` option resumes partial files.
Delete a file only if curl cannot resume it or if the size is larger than the
expected size.

If the GUI starts but colorization fails:

Check:

- CUDA is available in PyTorch.
- All model files match the expected sizes.
- `models\facebookresearch_dinov2_main` exists after extracting the zip.
- The user selected both a target and a reference in Image mode.

If port 7867 is busy:

Run from a command prompt with a different port:

    cd /d C:\GGF\GGF-CMNET2\GGF-CMNET2
    .venv\Scripts\python.exe app\app.py --port 7868


Final Result
------------
When finished, tell the user:

    GGF CMNET2 Colorization Studio is installed.
    Start it with:
    <DRIVE>:\GGF\GGF-CMNET2\CLICK-ME-TO-RUN.bat

Do not leave the user with only a GitHub URL.
Do not leave the user with only a cloned source folder.
Leave them with an installed app and a clear click-to-run file.
